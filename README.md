# Capital

[Frontend React] → /agent/run → [ASP.NET AgentService] → POST → [n8n webhook workflow]
                                                      ← JSON ←
                                             [Respond to Webhook in n8n]
